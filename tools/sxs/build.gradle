plugins
{
	id "java"
	id "application"
}

description = "This utility is used to provide a view into " +
	"the native code translation by showing a side by side representation " +
	"of the input source code, the original byte code, and then the " +
	"generated native code."
mainClassName = "dev.shadowtail.sxs.Main"

dependencies
{
}
