// -*- Mode: Java; indent-tabs-mode: t; tab-width: 4 -*-
// ---------------------------------------------------------------------------
// SquirrelJME
//     Copyright (C) Stephanie Gawroriski <xer@multiphasicapps.net>
// ---------------------------------------------------------------------------
// SquirrelJME is under the Mozilla Public License Version 2.0.
// See license.mkd for licensing and copyright information.
// ---------------------------------------------------------------------------
package cc.squirreljme.runtime.lcdui.event;

import cc.squirreljme.runtime.cldc.annotation.KeepWhenCompacting;

/**
 * Enumeration of vendor keys for use within the keycode translators.
 *
 * @since 2026/05/22
 */
@KeepWhenCompacting
enum __VendorKeys__
{
	/** Enum indicating the Back/CLR key. */
	KEYBACK,

	/** Enum indicating the Center/Fire key. */
	KEYCENTER,

	/** Enum indicating the Down Arrow key. */
	KEYDOWN,

	/** Enum indicating the Left Arrow key. */
	KEYLEFT,

	/** Enum indicating a vendor-specific Numpad 0 key. */
	KEYNUM0,

	/** Enum indicating a vendor-specific Numpad 1 key. */
	KEYNUM1,

	/** Enum indicating a vendor-specific Numpad 2 key. */
	KEYNUM2,

	/** Enum indicating a vendor-specific Numpad 3 key. */
	KEYNUM3,

	/** Enum indicating a vendor-specific Numpad 4 key. */
	KEYNUM4,

	/** Enum indicating a vendor-specific Numpad 5 key. */
	KEYNUM5,

	/** Enum indicating a vendor-specific Numpad 6 key. */
	KEYNUM6,

	/** Enum indicating a vendor-specific Numpad 7 key. */
	KEYNUM7,

	/** Enum indicating a vendor-specific Numpad 8 key. */
	KEYNUM8,

	/** Enum indicating a vendor-specific Numpad 9 key. */
	KEYNUM9,

	/** Enum indicating a vendor-specific Numpad '#'' key. */
	KEYPOUND,

	/** Enum indicating the Right Arrow key. */
	KEYRIGHT,

	/** Enum indicating the Left Soft key. */
	KEYSOFT1,

	/** Enum indicating the Right Soft key. */
	KEYSOFT2,

	/** Enum indicating a vendor-specific Numpad '*'' key. */
	KEYSTAR,

	/** Enum indicating the Up Arrow key. */
	KEYUP,

	/** Enum indicating the amount of vendor keys that can be mapped. */
	VENDORKEYNUM,

